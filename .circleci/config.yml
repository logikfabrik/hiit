version: 2.1

orbs:
 android: circleci/android@0.2.0

jobs:
  build:
    executor: android/android

    working_directory: ~/src

    steps:
      - checkout
      - run:
          command: gradlew build
